# DATA PROCESSING FILES

This folder contains a Python file that walks through the entire IR file and reconstructs  the evolution of IR graph edges across optimization phases using a json file.

This tool takes the raw IR dump produced by the JIT compiler and rebuilds the edge list for every optimization phase.